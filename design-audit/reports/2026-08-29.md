# AGIL Mobile — Component Watch Report — 2026-08-29

**Source:** [AGIL-Mobile-App / Mobile Component Library](https://www.figma.com/design/e6RBhSWdLlLinhceozYLYe/AGIL-Mobile-App?node-id=34-995) · **Baseline:** AOH UI/ShadCN · **Mode:** read-only (no changes made)

## Summary
Since the 2026-08-16 baseline, the watched node has undergone **major undocumented structural
churn**. Most seriously, the entire **▚ Map section** (~20 components: Map Pins, Bottom Drawer
Menu, Geofence/POI/Create-Incident forms, Map FAB, Map Canvas, etc.) and the **entire "chat bubble"
family** (Text/Image/Video/Location bubbles + fragmented audio/voice symbols) are **no longer
children of the tracked `34:995` node** — the section frame itself is gone from that subtree.
Spot-checks by direct node ID confirm these components **still exist in the file** (not deleted),
so they were detached/moved outside the governed structure rather than removed — but the "single
source of truth" library at `34:995` no longer contains them. The stale header label
(`"113 components · 9 modules ... Map ..."`) still advertises a Map module that isn't there. On
the positive side, several components gained real states this run (Incident/Card "Just Added",
Mobile BG Overlay "Blur", a proper 8-variant Keyboard set), and the long-open **"My response" raw
purple stroke** adherence flag is confirmed **resolved** (now correctly bound to `color-border`).

| Detached sections | Detached components | New/upgraded | Corrupted names | Persisting empty sections | Adherence resolved |
|-------------------:|---------------------:|-------------:|------------------:|---------------------------:|---------------------:|
|         1           |          ~28          |       5       |         1          |             2               |          1            |

## 🚩 Needs flagging — structural (this run)

| # | Pri | Evidence (id · name) | Concern | Note |
|---|-----|----------------------|---------|------|
| 1 | **P1** | `3975:2050` **▚ Map** section | The entire Map section frame is **absent from `34:995`'s child tree** (confirmed via direct `get_metadata` on `34:995` — Foundations→Global→Shared→Comms→Chat→Incident jump straight to a loose icon symbol, then Resources→Login; no Map section wrapper at all, unlike the merely-empty Comms/Resources sections which retain their frame). Spot-checked 2 of its known components directly by id: `3654:50515` "Map Pins" and `3316:43247` "Map/ Bottom Drawer Menu" **both still exist and still hold their variant/state structure** — they were not deleted, just detached from the tracked node. Remaining ~18 Map components (Geofence Shape, Dispatch Mini Map, POI/Delete/Geofence/Create-Incident forms, Snackbar, Mobile Options, Icon/Camera, Map Options, Map FAB, Map Canvas, map Browse, status chip, Full Map Header, video feed area, Mobile Badge, Segmented Control) were **not individually re-verified this run** — their fate (also detached vs. actually deleted) is unconfirmed. | Needs designer input: was this an accidental drag-out of the section, or a deliberate move in progress? Either way `34:995` — the file everything downstream treats as canonical — is currently missing an entire module. |
| 2 | **P1** | Chat-bubble family (`129:142` was "Chat / Bubble", `185:4365` "Chat / Bubbles", `3365:58368` "Chat/ Video Bubble", `3917:5922` "Chat / Location Bubble", plus the 6 ungrouped audio/voice symbols flagged 2026-08-16) | **None of these appear under `3975:2046` (Chat / grid) any more.** Direct id lookup on `129:142` shows it **still exists**, but renamed to **"Chat / Text Bubble"** with a reworked variant set (`User=Recipient\|Sender` × `Type=Incoming\|Outgoing\|Outgoing-Sending\|Deleted`) — this looks like the start of the consolidation this audit recommended on 2026-08-16 — but it is **positioned outside the Chat section's grid frame**, not filed back in. The other bubble variants (Image/Video/Location/audio/voice) were **not individually re-verified this run**. | If consolidation is underway, good — but it needs to land back inside `3975:2046` under one namespace so the tracked structure reflects reality. Needs verification next run on the remaining bubble types. |
| 3 | **P2** | `97:2007` — was "Description Flow" (Incident tier, 2026-08-16 baseline) | Now sits inside **▚ Foundations** with a corrupted, repeated-path name: `"Comms / Call Log Row/Comms/Call Log Row/Comms/Call Log Row/Comms/Call Log Row/Recent/Atom/Description Flow"`. Looks like an accumulated rename artifact (the same path segment duplicated 4×) rather than an intentional name. | Rename to something meaningful (e.g. `Comms / Call Log Row / Description Flow`) and confirm Foundations is the right home for it — it doesn't obviously belong with the empty Foundations section's other occupant (Annotation). |
| 4 | **P3** | `4075:64536` **"Icon/ Drone"** (loose symbol, floats directly under `34:995` root, not inside any section, near y=10898 between Incident and Resources) | This is a **different node id** from the baseline's Map-tier "Icon/ Drone" (`3327:56143`), i.e. a **second, orphaned copy** of the same icon sitting outside all sections. | Confirm which one is canonical, delete/merge the duplicate, and file it inside a section (Global icons, most likely) rather than loose on the canvas. |
| 5 | **P3** | `1802:3142` icon/x, `1802:3146` icon/send, `132:189` "Mobile / Keyboard" (old single-state component) | Not found under `3975:2037` (Global / grid) this run. `Mobile / Keyboard` appears superseded by a proper new **`Keyboard / Default`** set (`1693:36527`, 8 variants: Type=Lower/Upper/Numerals/Numbers × Theme=Light/Dark) — a genuine improvement if intentional. `icon/x` and `icon/send` have no obvious successor and were not re-located elsewhere in this pass. | Confirm icon/x and icon/send weren't accidentally dropped — chat/system UI likely still needs them. |
| 6 | **P2** | `3975:2043` "Comms / grid", `3975:2055` "Resources / grid" | **Still empty for the 3rd consecutive audit** (2026-07-01 gap noted, flagged 2026-08-16, unresolved 2026-08-29). `comms/avatar-surface` and `comms/bar-text-primary` remain bound within `34:995` per `get_variable_defs`, and a new **`PTV Feed`** component set (`3233:32953`, 5 states: Own/Other Feed × Default/Live, Off State) has appeared this run — but it's filed under **Chat**, not Comms, so it doesn't resolve the empty-Comms question. | This has now gone unresolved across 3 audits — worth escalating past "needs verification" to an explicit ask: is Comms mid-migration, or should the empty section be removed and PTV Feed's home (Chat vs Comms) be decided deliberately? |

## 🔀 Undocumented moves (still inside `34:995`, different section — verified via metadata)

| Component | Was (2026-08-16) | Now |
|---|---|---|
| `823:14770` Content | Shared | Incident |
| `713:38347` Dispatch Modal | Login | Incident |
| `3207:44277` Annotation | Shared | Foundations |
| `3575:22945` Mobile Search | Map | Shared — also gained a new `Configuration=Inputted` state |
| `305:4285` (was "Chat / Selection Header") | standalone symbol, Chat | now nested in a generic **"Chat"** frame (`4035:83121`) as variant `State=Selection Top Bar` — lost its descriptive name in the process |

None of these moves are logged anywhere (no changelog); the file header text (`3987:123`) still reads "113 components · 9 modules — Foundations · Global · Shared · Comms · Chat · Incident · Map · Resources · Login" and has not been updated to reflect any of this — it's now inaccurate on both the module list (Map is gone) and likely the count.

## ✅ Adherence — resolved since last run
- `937:29991` **My response** — the raw `#8a38f5` purple stroke flagged 2026-07-01 and carried as "unverified" through 2026-08-16 is **confirmed fixed**: `get_design_context` shows the border now uses `var(--color-border, #e4e4e7)` consistently across all 8 state variants, with fills/text properly on `color-bg-elevated`, `color-text-muted`, `color-text-success/warning/interactive`. No raw hex found in this component.

## 📐 Design-system adherence (AOH UI/ShadCN) — not re-verified this run
- `1058:616` Resource / Responder Row — still not locatable under `34:995` (consistent with the missing Resources section); raw-colour flag from 2026-07-01 remains unconfirmed either way.
- Mobile/Button pressed state (raw 14% black state-layer) — not re-checked this run; carry forward as open.
- `get_variable_defs` on `34:995` returns a clean, large set of properly-named AOH tokens (color-*, Spacing/*, Radius/*, text-*/font-*) with no stray raw-hex entries in the bound-variable list itself — this only confirms what *is* bound, not undetected raw values elsewhere, so it isn't a full adherence pass.

## 📱 Mobile spec (M3) compliance — spot-check from `get_metadata` sizes
- **Touch targets ≥48dp:** all interactive components sized in this pass check out — Filter Chip 70×48, Icon Button (Mobile) 56×56, Mobile/Button 76×48 (Default) / 98–122×56 (Critical), Nav/Tab Item 56×48, Checklist/Item 360×48, Incident/Tabs 360×48, Action Tile 88×64 (Square) / 64×64 (Circle), My response pills 102×62, Chat/File Row 360×60. No sub-48dp interactive targets found in the components inspected.
- **Advisory (not a violation):** `Avatar` symbols (`1980:1715` set) are 40×40dp. If avatars are tappable (open profile/roster), the visual size is fine but the **hit area** should still extend to 48dp via padding — Figma's bounds don't show hit-slop, so this can't be confirmed either way from metadata alone.
- **Body type / spacing / radius:** spot-checked via `937:29991` — uses `text-sm` (14sp) consistently, `Spacing/spacing-2xs/sm` and `Radius/radius-dialog` tokens, all on-grid. Not exhaustively scanned across all 113 components this run (budget); no sub-12sp text or off-grid spacing found in what was checked.
- **Safe-area / grid spot-checks:** not performed this run — carry forward to next audit.

## ✏️ What can change (advisory — not applied)
- Re-file the Map section and the chat-bubble family back under `34:995` (or, if the moves were deliberate, update `design-audit/config.json`'s `watchedNodeId` scope and this file's header label to match reality) — right now the canonical library is materially incomplete.
- Finish the "Chat / Text Bubble" consolidation (the renamed `129:142`) by bringing the Image/Video/Location/audio/voice variants into the same family, then re-file the whole set inside `3975:2046`.
- Fix the corrupted name on `97:2007` and decide its real home.
- Resolve the 3-audit-old empty Comms/Resources sections — at minimum, decide where `PTV Feed` (`3233:32953`) belongs.
- De-duplicate the two "Icon/ Drone" symbols (`3327:56143` vs `4075:64536`).
- Regenerate the header label (`3987:122`/`3987:123`) once the structure stabilizes — it's actively misleading right now.

## ❓ Open questions for the designer
- Was the Map section detachment and chat-bubble detachment intentional (e.g. mid-reorganization in a different tool/branch) or accidental?
- Is `PTV Feed` meant to live in Comms or Chat going forward?
- Are `icon/x` and `icon/send` still needed, or superseded by something not surfaced in this pass?

---
Pri: **P1** blocks/violates · **P2** meaningful inconsistency · **P3** polish. Findings cite node ids as evidence; anything not re-verified this run is stated explicitly rather than assumed unchanged.
