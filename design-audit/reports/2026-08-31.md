# AGIL Mobile — Component Watch Report — 2026-08-31

**Source:** [AGIL-Mobile-App / Mobile Component](https://www.figma.com/design/e6RBhSWdLlLinhceozYLYe/AGIL-Mobile-App?node-id=34-995) · **Baseline:** AOH UI/ShadCN · **Mode:** read-only (no changes made)

## Summary
No structural change since the 2026-08-29 baseline: `get_metadata` on `34:995` returns the same
59 top-level components/sets with identical ids, names, and variant lists, and `get_variable_defs`
returns the same clean AOH token set (no new raw values, no new local tokens beyond the tracked
`comms/avatar-surface` / `comms/bar-text-primary`). Every issue flagged on 2026-08-29 **persists
unresolved and unchanged** — nothing has regressed further, but nothing has been fixed either.

| New | Removed | Renamed | Duplicates | Flags |
|----:|--------:|--------:|-----------:|------:|
|  0  |    0    |    0    |     1 (carried) | 9 (carried) |

## 🆕 New since last run
- _(none — structure byte-identical to 2026-08-29)_

## 🗑️ Removed since last run
- _(none)_

## 👯 Duplicates & fragmentation
| # | Pri | Components (ids) | Issue | Suggested change |
|---|-----|------------------|-------|------------------|
| 1 | P3 | `4075:64536` "Icon/ Drone" (loose, root-level) vs `3327:56143` "Icon/ Drone" (Map tier) | Two copies of the same icon; unresolved for 3+ audits | Confirm canonical copy, delete the other, file inside a section |

## 🚩 Needs flagging (all carried forward, unchanged)
| # | Pri | Evidence (id · name) | Concern | Note |
|---|-----|----------------------|---------|------|
| 1 | **P1** | `3975:2050` **▚ Map** section | Still entirely absent from `34:995`'s child tree (confirmed again via `get_metadata`; no section wrapper). Not re-verified by direct id lookup this run (no metadata change to prompt re-checking); status assumed unchanged from 2026-08-29's spot-check (components still exist elsewhere, just detached). | Open since 2026-08-16; unresolved 3rd+ run. |
| 2 | **P1** | Chat-bubble family (`129:142` "Chat / Text Bubble" + Image/Video/Location/audio-voice variants) | Still not present under `3975:2046` (Chat / grid) — metadata for that frame is unchanged from 2026-08-29. | Unresolved 3rd+ run. |
| 3 | **P2** | `97:2007` (Foundations tier) | Still carries the corrupted repeated-path name `"Comms / Call Log Row/Comms/Call Log Row/Comms/Call Log Row/Comms/Call Log Row/Recent/Atom/Description Flow"` — unchanged. | Unresolved 3rd+ run. |
| 4 | **P2** | `3975:2043` "Comms / grid", `3975:2055` "Resources / grid" | Both still empty — **4th consecutive audit**. `comms/avatar-surface` / `comms/bar-text-primary` still bound within `34:995` per `get_variable_defs`; `PTV Feed` (`3233:32953`) still filed under Chat, not Comms. | Escalating — 4 straight audits unresolved. |
| 5 | **P3** | `469:262` "Option Context menu" | Variant property still named generic `Property 1=Default`, never renamed to something descriptive. | Unresolved 3rd+ run. |
| 6 | **P3** | `2723:28132` "Chat Header" | Still no-slash naming (rest of Chat tier uses `Chat / X`). | Unresolved 3rd+ run. |
| 7 | **P3** | `4035:83121` "Chat" (frame wrapping `State=Selection Top Bar`) | Still a generic bare name; lost its descriptive "Chat / Selection Header" identity when nested. | Unresolved since 2026-08-29. |
| 8 | **P3** | `1802:3142` icon/x, `1802:3146` icon/send | Still not found under Global / grid (`3975:2037`); no successor confirmed. | Unresolved since 2026-08-16. |
| 9 | **P3** | `3987:123` header label | Still reads "113 components · 9 modules — … · Map · …" despite the Map module being absent and the count being stale. | Unresolved since 2026-08-16. |

## 📐 Design-system adherence (AOH UI/ShadCN)
- `get_variable_defs` on `34:995` returns the same clean, large set of properly-named AOH tokens
  (color-*, Spacing/*, Radius/*, text-*/font-*, Size/*, Shadows/*) with no stray raw-hex entries in
  the bound-variable list — consistent with 2026-08-29. This confirms what *is* bound, not undetected
  raw values elsewhere; no spot-check with `get_design_context` was performed this run since the
  component tree is unchanged from the run that already spot-checked it.
- `1058:616` Resource / Responder Row — still not locatable under `34:995` (consistent with the
  missing Resources section); the 2026-07-01 raw-colour flag remains unconfirmed either way.
- Mobile/Button pressed state (raw 14% black state-layer) — carried forward as open, not re-checked.

## 📱 Mobile spec (M3) compliance — per `mobile-rules.md`
No re-measurement performed this run: `get_metadata` sizes for every node are byte-identical to the
2026-08-29 snapshot, which was already spot-checked and found compliant (no sub-48dp interactive
targets found in the components inspected). Carried forward unchanged:
- Touch targets ≥48dp: no violations found in prior spot-check; not re-measured (no size change).
- Advisory (not a violation): `Avatar` symbols (`1980:1715` set) are 40×40dp visual size; hit-slop to 48dp can't be confirmed from metadata alone.
- Body type / spacing / radius: last spot-checked via `937:29991` (on-grid, tokenized); not exhaustively re-scanned.
- Safe-area / grid spot-checks: not performed this run — carried forward again.

## ✏️ What can change (advisory — not applied)
- Re-file the Map section and chat-bubble family back under `34:995`, or update `config.json`'s
  `watchedNodeId` scope and the header label to match reality — this has been open since 2026-08-16
  and is now unresolved across 4 audits.
- Decide `PTV Feed`'s home (Chat vs Comms) and resolve the empty Comms/Resources sections.
- Fix the corrupted name on `97:2007`.
- De-duplicate the two "Icon/ Drone" symbols.
- Regenerate the stale header label (`3987:122`/`3987:123`).

## ❓ Open questions for the designer
- Same three questions as 2026-08-29, still unanswered: was the Map/chat-bubble detachment
  intentional? Where does `PTV Feed` belong? Are `icon/x` / `icon/send` still needed?

---
Pri: **P1** blocks/violates · **P2** meaningful inconsistency · **P3** polish. Findings cite node ids as evidence; anything not re-verified this run is stated explicitly rather than assumed unchanged.
