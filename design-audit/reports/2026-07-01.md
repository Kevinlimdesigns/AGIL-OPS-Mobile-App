# AGIL Mobile — Component Watch · 2026-07-01

## Re-audit (later same day) — 73 components
Changes since the morning run (72 → 73):
- **New:** `1058:616` **Resource / Responder Row** (360×48, Resources tab). ⚠️ **Not AOH-bound** — raw
  `#09090b` fill + raw white stroke, **0 bound tokens**. **P2: rebind bg → `color-bg`, stroke → `color-border`.**
- **Changed (variants):** `124:72` **Action Tile** 2 → **4** (+`State=Disabled`, 40% opacity);
  `937:29991` **My response** 4 → **8** (+`Disabled=False/True`, disabled = 40% opacity). Both requested;
  built + verified this session.
- **Adherence — still open:** `937:29991` My response raw stroke `#8a38f5` (purple) — now on all 8 variants.
- **Duplicates / fragmentation / M3 status:** unchanged from the run below (Talk bar sandbox duplicate;
  button + header families; M3 P1 batch pending).
- **Net adherence flags now: 3** — Resource/Responder Row (unbound), My response (purple stroke),
  Mobile/Button pressed (raw state-layer). All P2/low.

---


**Watched:** `34:995` "Mobile Component" (Components page) · **Baseline:** 2026-06-26 (69) → **now 72**.
Read-only audit. Evidence = node id + measured value.

## Summary
- **New:** 4 · **Removed:** 1 · **Changed (variants):** 3 · **Duplicates/fragmentation:** 3 · **Adherence flags:** 2 · **Mobile/M3:** mostly aligned (nav + talk-bar work landed this cycle).

## New (4)
| id | name | notes |
|----|------|-------|
| `823:14770` | **Content** (incident detail info block) | 320w, VERTICAL, 16 pad, **8 gap**; now the single source for the incident info block — replicated to 4 Detail pages. Fully token-bound ✓ |
| `937:29991` | **My response** (set: Accepted/Enroute/On scene/Clear) | Incident sticky-zone status selector. ⚠️ 1 raw stroke (see Adherence). |
| `713:38347` | **A5 · Dispatch Brief — AOH DS** | Dispatch brief card (was an on-screen instance). Fully token-bound ✓ |
| `795:6272` | **Grid - Android** | 360×640 layout-grid utility (guide overlay), raw white fill — utility, exempt. |

## Removed (1)
- `82:553` **Incident / Assignment Prompt** — no longer within the watched node. *Verify: deleted intentionally, or moved out of `34:995`?*

## Changed (variants)
- `68:1219` **Nav / Tab Item**: 2 → **4** variants (+`Active-Pressed`, +`Inactive-Pressed`). M3 alignment this cycle (pill 56×32 r16, icon 24, item 64, 10% pressed state layer).
- `185:4365` **Chat / Image Bubble**: 2 → **4** (+`User Caption`, +`Own Caption`).
- `513:445` **Contact chip**: 4 → **5** (+`Status=Add`).
- `439:261` **Mobile / Button**: now enumerated as 8 variants (State ×4 × Size ×2) — consistent with baseline.

## Duplicates / fragmentation
1. **Talk bar — States duplicated.** `466:8232` (canonical, in `34:995`) **and** `831:11113` (copy in the "Talk Bar State" section `831:11112`, *outside* the watched node) + 5 variation frames + a tonal-wash exploration (`859:3`, `861:3`). Sandbox is intentional (design review); **consolidate to one set once the tonal direction is picked.**
2. **Button family.** `439:261` Mobile/Button vs `524:15271` PTV/Control Button — still separate definitions (open item).
3. **Headers ×4.** `53:1027` Header · `129:111` Chat/Header · `514:262` Comms/Header · `126:70` Incident/Detail Header — review for shared base.

## Adherence (AOH tokens)
- ⚠️ **`937:29991` My response** — 1 **raw stroke `#8a38f5`** (purple, not an AOH token; looks like a stray/selection colour). **P2: rebind to an AOH `color-*` or remove.**
- ℹ️ **`795:6272` Grid - Android** — raw white fill; it's a grid guide utility, **exempt**.
- ✓ **Resolved this cycle:** Comms/PTV raw-hex → tokens; nav pill + **badges → `severity/critical` (red)**; SOS solid-red regression (fill `severity/critical` @0.08); Talk bar on-colour overlays → `comms/*`.
- ℹ️ **Sanctioned exceptions (documented):** `comms/*` on-colour overlay tokens (talk bars); nav-label +0.5px tracking (M3 nav ergonomics).
- ◻ **Open:** Mobile/Button pressed uses raw 14% black — no AOH state-layer token.

## Mobile spec (M3) compliance
- ✓ **Nav / Tab Item** — item 64, pill 56×32 r16, icon 24, pressed state layer (M3-aligned, per `m3-navigation-benchmark.md`).
- ✓ **Talk bar — States** — row gaps standardized to **16** (all 10/12 removed); 4-colour reduction proposed (tonal-wash exploration built for review).
- ✓ **Badges** — red (`severity/critical`) = M3 default badge colour; stand out from the blue active state.
- ◻ **Content (`823:14770`)** — 8 gap / 16 pad consistent; note it sits **320w at x0** on 360 screens (≈40px right gap, inherited from the Closed-screen design — align to 360/centred if undesired).
- ◻ **Broader M3 value-alignment** (buttons/chips/cards/list-items/tabs/app-bars/sheets/dialogs/inputs) — planned, not executed (see `m3-alignment-plan.md`, P1 batch first).
- ◻ **Verify:** `My response` option rows ≥ 48 dp touch target (dropdown 152×348 / 4 options + header).

## What can change (advisory)
1. Rebind/remove the raw purple stroke in **My response** (`937:29991`).
2. **Consolidate** the Talk bar — States duplicate once the tonal-wash direction is confirmed; then sweep app instances.
3. Execute **M3 alignment P1** (list-item 56/72/88 ladder; chips 32-visual-in-48-target).
4. **Button unification** (Mobile/Button vs PTV/Control Button) + AOH state-layer token.

## Open questions
- Is `82:553` Incident/Assignment Prompt removal intentional?
- Confirm tonal-wash as the canonical talk-bar treatment (then reduce to 4 states/colours + sweep).
