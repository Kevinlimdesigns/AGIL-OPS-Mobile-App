# AGIL Mobile — Component System Audit — 2026-06-30

**Scope:** [Mobile Component](https://www.figma.com/design/e6RBhSWdLlLinhceozYLYe/AGIL-Mobile-App?node-id=34-995) (node `34:995`, 69 components in Global/Shared/Module-Specific) · **Baseline:** AOH UI/ShadCN · **Mobile ref:** Material 3 · **Mode:** read-only (no changes by this audit)

## Summary
Strong, stable state. **69 top-level components, 0 duplicate names**, organized into the 3 labelled sections, and **measurably more on-system** than prior runs: typography now bound to AOH text **styles**, status colours governed by `severity/*` tokens, touch targets met, all components 360 dp. No new/removed components since the 2026-06-26 baseline — recent work was internal (typography, button sizes, token rebinds).

| New | Removed | Dup names | Open duplicates/frag | Open adherence | Open mobile |
|----:|--------:|----------:|---------------------:|---------------:|------------:|
|  0  |    0    |    0      |          4           |       4        |      2      |

## ✅ Verified improvements
- **Typography → AOH styles:** 9 primary-content nodes now use the **`text-base/font-regular`** style (16 sp, governed) — confirmed `styled:true, size:16`. (Raw sizes eliminated.)
- **Severity tokens:** `severity/info|normal|caution|serious|critical` present (all ≥4.5:1 on dark); Talk bar — States rebound.
- **Buttons:** `Mobile / Button` = State × Size (Default 48 / **Critical 56**), 8 variants.
- **Touch targets:** Filter Chip 48 · Checklist 48 · Status Banner 48 · Detail/Field 52 (grew with 16 sp value) — all ≥48 ✓.
- **Width:** all components 360 dp; **all 94 screens fit 360×780**.
- **Checkbox radius** tokenized; **3 skeleton archetypes** built (List/Detail/Comms).

## 👯 Duplicates & fragmentation (open)
| # | Pri | Components | Issue | Suggested change |
|---|-----|-----------|-------|------------------|
| 1 | P2 | `Chat / File Bubble` + `Chat / File Bubble Uploading` | Differ only by upload state | Merge via a `State` prop |
| 2 | P2 | Chat / Bubble · Voice Bubble · Image Bubble · File Bubble (+Uploading) | Bubble family fragmented | Consolidate into one `Type × Direction × State` set |
| 3 | P3 | Chat / Image Bubble → `Type=Crime` | Content as a variant | Make it a content example |
| 4 | P3 | `Header` (Global) vs Chat / Header vs Incident / Detail Header vs Comms / Header | 4 headers — generic `Header` overlaps | Verify generic `Header` still used; archive if orphaned |

## 📐 AOH adherence (open)
- **Button definitions in 3 places:** `Mobile / Button` vs raw **AOH ShadCN Button** instances (on screens) vs `PTV / Control Button` — unify on `Mobile/Button`; add `Type` incl. destructive so PTV reuses it.
- **`comms/*` local tokens** still defined outside AOH — promote or formally sanction.
- **Pressed state-layer:** `Mobile/Button` Pressed uses raw 14% black — add an AOH `state-layer` token.
- **Remaining status components not yet on `severity/*`:** PTT Controls, Live Badge, PTV Incoming Banner, Incident Status Banner (only Talk bar — States rebound so far).

## 📱 Mobile spec (M3) (open)
- **PTT / SOS 64 dp size** not yet added (Mobile/Button has 48/56; the highest-stakes controls should be 64).
- **Icon-button 48 dp wrappers** — verify PTV/icon and other tappable icons sit in ≥48 dp targets.

## 🚩 Other
- **`icon/user` (39:18)** still 0 usage — deletion candidate.
- **Title weight:** per your instruction, the bound titles are now **regular** (text-base/font-regular). If you want titles emphasised again, switch those nodes to `text-base/font-medium` or `font-bold`.

## ✏️ Recommended next (advisory)
1. Rebind remaining status components → `severity/*`.
2. Add `Type` (incl. destructive) to `Mobile/Button` + a 64 dp PTT/SOS size; migrate AOH Button instances.
3. Govern `comms/*`; add a pressed `state-layer` token.
4. Merge File Bubble + Uploading; consolidate the bubble family; archive the orphan `Header`.

---
Pri: **P1** blocks/violates · **P2** meaningful inconsistency · **P3** polish. Findings cite component names. No Figma changes made by this audit.
