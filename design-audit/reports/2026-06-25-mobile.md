# AGIL Mobile — Mobile Compliance Pass #1 — 2026-06-25

**Scope:** [AGIL-Mobile-App / Mobile Component](https://www.figma.com/design/e6RBhSWdLlLinhceozYLYe/AGIL-Mobile-App?node-id=34-995) (node `34:995`) · **Benchmark:** Material 3 (ergonomics only) per [`mobile-rules.md`](../mobile-rules.md) · **Target:** Android / Kotlin / Jetpack Compose · **Mode:** read-only (no changes made)

## Summary
First mobile-compliance pass of the shadcn-built mobile components against the M3 thresholds.
**Typography and spacing are healthy**; the real gap is **touch targets** — chips, checklist rows,
and one field state are sized from desktop ShadCN and fall under the 48 dp minimum. One off-scale
radius and missing pressed/disabled states are minor. Safe-area + grid are screen-level and checked
in the flow section, not here.

| Check | Result |
|---|---|
| Typography ≥14 sp | ✅ Pass (sampled all 14 sp Geist Medium) |
| Spacing on 4/8 dp grid | ✅ Pass |
| Touch targets ≥48 dp | ✅ Remediated (4 components + 8 instances → 48 dp) |
| Radius bound to AOH `Radius/*` | ✅ Checkbox radius bound to `Radius/radius-round` |
| Pressed/disabled states | ⚠️ Missing (P3) |
| Safe-area insets / 4-col grid | ↗︎ Screen-level (section 337:4323) |

## 📱 Mobile spec (M3) compliance
| # | Pri | Evidence (id · name · measured) | Rule | Fix |
|---|-----|---------------------------------|------|-----|
| 1 | P1 | `127:105/107` Filter Chip · **28 dp tall** | Touch target ≥48 dp | min-height 48 (or 32 visual pill in 48 tap frame) |
| 2 | P1 | `187:4…` Chat Quick Reply chips · **~28 dp** (row 40 dp) | Touch target ≥48 dp | same chip fix; grow row to 48 |
| 3 | P2 | `114:62/65` Checklist Item · **44 dp** | Touch target ≥48 dp | row min-height 48 |
| 4 | P2 | `112:60` Detail/Field Badge=False · **44 dp** | Touch target ≥48 dp | 48 (Badge=True `112:63` already 48 ✓) |
| 5 | P3 | `114:63` Checklist checkbox · **radius 10 dp** | Bind to AOH `Radius/*` | use `radius-sm` (4) / `radius-input` (6), or add a token |
| 6 | P3 | Interactive components generally | Provide pressed/disabled | add pressed/disabled state variants |

## 🔧 Remediation applied (2026-06-25, approved)
All edits in place on the canonical components (instances inherit), values stay token-bound:
- **Filter Chip** `127:105/107` — restructured to a **48 dp transparent tap frame** wrapping a **32 dp pill** (M3-faithful). **8 Filter Chip instances** across the Components page refreshed to 48 dp; **Chat Quick Replies** row now 60 dp (48 dp targets + padding), pills render slim and centered.
- **Checklist Item** `114:62/65` — 44 → **48 dp**.
- **Detail/Field Badge=False** `112:60` — 44 → **48 dp** (matches Badge=True).
- **Status Banner** `187:3977/3979` — 32 → **48 dp** (treated as **tappable** per design intent).
- **Priority / Pill** `77:243–249` — **left as-is** (confirmed display-only, exempt).
- **Checkbox radius** `114:63` / `114:66` — **tokenized**: bound all four corners to `Radius/radius-round` (located via in-file usage). Circle preserved, now on-system.

Result: **0 touch-target violations remaining** and checkbox radius tokenized. Open: **pressed/disabled/loading** states for buttons (+ selected for toggles), and icon-button 48 dp wrappers.

## ✅ Passing
- **Type:** Filter Chip, Checklist Item, Quick Replies, Incident Tabs all render at **14 sp** (≥14 sp).
- **Spacing:** px-12/py-4, gap-12, px-16/py-12, gap-8 — all on the 4/8 dp grid.
- **Already compliant targets:** Nav Tab Item 56 · Incident Tabs 48 · Responder Row 60 · Action Tile 60/64 · Chat Input Bar 56 · Photo Tile 84 · Tab/Talk bar 80/72 · File Row 60.

## ⚠️ Verify (intent needed)
- `77:243–249` **Priority / Pill** (24 dp) and `187:3981` **Status Banner** (32 dp): exempt **if display-only** (they read as status indicators, not tappable). Confirm.
- **Icon buttons:** raw icons are 24 dp — ensure each tappable icon sits in a **48 dp wrapper**.
- **Safe-area insets & 4-col/16 dp grid:** screen-level — evaluated on the flow screens (`337:4323`), not this component page.

## ✏️ Recommended next (advisory — not applied)
1. Raise the 4 failing touch targets to ≥48 dp (items 1–4) — biggest ergonomic win.
2. Tokenize the checkbox radius (item 5).
3. Add pressed/disabled states (item 6) as a follow-up batch.

---
Pri: **P1** target <40 dp / body <12 sp / missing safe-area · **P2** target 40–47 dp / off-grid · **P3** polish. Measured values from `get_metadata` + `get_design_context`. No changes made to the Figma file.
